# Plan: Add console print output for JSON scenarios and config JSON

## Context

The file `scenario_conversation_app_vocal_only_fixed.py` (19,651 lines) contains multiple stacked versions (all commented out) with the **active/uncommented code** starting at line ~17253. The active code has two key JSON structures:

1. **`scenario_json`** — a `Dict[str, Any]` generated by the LLM (via `_generate_via_llm()` at line 19138, or via `_fallback_static_scenario()` at line 19208). Contains keys: `titre`, `objectifs`, `grandes_lignes`, `procedures_cles`, `points_vigilance`, `resume_oral`.

2. **`scenario_text`** — a `str` derived from `scenario_json` by `_json_to_text()` (line 19170). A human-readable markdown version.

3. **`config`** (LLM config) — returned by `_setup_llm()` (line 19565) via `build_llm_config()`. An object with attributes like `token`, `api_url`, `model`.

There is already a method `_print_scenario_debug()` (line 19253) that prints **both** `scenario_text` and `scenario_json` to the console — but it is only called from `_announce_scenario()` (line 19283), which runs **once** after generation. The task is to ensure these are also printed every time they are **created or accessed**.

## Affected Locations (all in the active, uncommented code)

### 1. Scenario JSON creation — `_generate_via_llm()` (line 19138)
After successfully parsing the LLM response into `obj` (line 19159) and converting to text (line 19166), add a print block outputting both `obj` (JSON) and `text`.

### 2. Scenario JSON creation — `_fallback_static_scenario()` (line 19208)
After building the static `obj` dict (line 19210–19249) and converting to text (line 19250), add a print block outputting both `obj` (JSON) and `text`.

### 3. Scenario JSON access — `_generate_scenario()` (line 19097)
After receiving `scenario_json` and `scenario_text` from either `lib_generate` or `_generate_via_llm`, add a print block to log what was returned (including the JSON if present).

### 4. Config JSON creation — `_setup_llm()` (line 19565)
After `build_llm_config()` returns `config`, add a print statement outputting the config as a dict (config is an object with attrs like `token`, `api_url`, `model` — use `config.__dict__` or construct a safe dict excluding secrets). **Note:** Do NOT print the `token` value in plain text — mask it or skip it.

### 5. Config JSON creation (fallback path) — already handled
No fallback `_setup_llm` returns `None`; no extra print needed beyond the warning log that already exists.

### 6. Scenario JSON access in `_build_scenario_session()` (line 19120)
After constructing the `ScenarioSession` at line 19124, pass `scenario_json` through to the session. Add a print outputting the `scenario_json` dict to show what was stored. This is the "access" when the session is built.

### 7. Scenario JSON access in Q&A — `_answer_via_direct_llm()` (line 19447)
When `scenario_session.scenario_text` is accessed at line 19452 to build the LLM prompt context, the `scenario_json` is also available on the session. Add a print block outputting the `scenario_json` from `scenario_session` before constructing the user message.

## Tasks (in order)

1. **In `_generate_via_llm()` (line ~19166)**: After `text = self._json_to_text(obj, topic)` and before `return obj, text`, add:
   ```
   print("\n" + "=" * 60)
   print("  SCÉNARIO GÉNÉRÉ (JSON)")
   print("=" * 60)
   print(json.dumps(obj, indent=2, ensure_ascii=False))
   print("=" * 60)
   print("\n" + "=" * 60)
   print("  SCÉNARIO TEXTUEL")
   print("=" * 60)
   print(text)
   print("=" * 60 + "\n")
   ```

2. **In `_fallback_static_scenario()` (line ~19250)**: After `text = VRScenarioApp._json_to_text(obj, topic)` and before `return obj, text`, add the same print block as above.

3. **In `_generate_scenario()` (line ~19113)**: After each `return` path that provides non-empty `scenario_json`, add the print block before returning. Specifically:
   - Before `return scenario_json, scenario_text` at line 19114
   - Before `return self._generate_via_llm(topic, config)` at line 19113 (the inner call will handle printing)
   - Before `return self._generate_via_llm(topic, config)` at line 19117 (same)

4. **In `_setup_llm()` (line ~19567)**: After `return build_llm_config()`, capture the return value, print it (masking the token), then return it. Add before the return:
   ```
   print("\n" + "=" * 60)
   print("  CONFIG LLM")
   print("=" * 60)
   _cfg = config.__dict__.copy()
   if "token" in _cfg and _cfg["token"]:
       _cfg["token"] = "***" + str(_cfg["token"])[-4:]
   print(json.dumps(_cfg, indent=2, ensure_ascii=False, default=str))
   print("=" * 60 + "\n")
   ```

5. **In `_build_scenario_session()` (line ~19131)**: After `session = ScenarioSession(...)` and the logger.info, before `return session`, add:
   ```
   print("\n" + "=" * 60)
   print("  SCÉNARIO STOCKÉ DANS LA SESSION (JSON)")
   print("=" * 60)
   print(json.dumps(scenario_json, indent=2, ensure_ascii=False))
   print("=" * 60 + "\n")
   ```

6. **In `_answer_via_direct_llm()` (line ~19447)**: After extracting the context from `scenario_session.scenario_text`, add a print of `scenario_json`:
   ```
   if scenario_session and hasattr(scenario_session, 'scenario_json') and scenario_session.scenario_json:
       print("\n" + "=" * 60)
       print("  SCÉNARIO RÉFÉRENCE (JSON) — Q&R")
       print("=" * 60)
       print(json.dumps(scenario_session.scenario_json, indent=2, ensure_ascii=False))
       print("=" * 60 + "\n")
   ```

## Notes

- Use `json.dumps(..., indent=2, ensure_ascii=False)` for all JSON output (consistent with existing `_print_scenario_debug`).
- All additions are pure `print()` statements — no changes to logic, no added imports needed (`json` is already imported at line 17316).
- The `scenario_text` already has a dedicated print in `_print_scenario_debug` which is called from `_announce_scenario`. The prints in tasks 1 and 2 cover the creation paths before `_announce_scenario` is reached.
- Mask the LLM `token` in config output — never print secrets in plain text.
